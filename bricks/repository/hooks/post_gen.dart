import 'package:mason/mason.dart';

import '../../../cli/cli.dart';

void run(HookContext context) async {
  //final logger = context.logger;
  //await Dart.format(logger: logger);
  //await Dart.applyFixes(logger: logger);
}
